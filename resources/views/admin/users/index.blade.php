@extends('layouts.app')

@section('content')
<div class="card">
    <div class="card-header">{{ __('Administración del blog') }}</div>

    <div class="card-body">
        {{ __('Estás logueado correctamente') }}
    </div>
</div>
@endsection
